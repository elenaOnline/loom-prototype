{
  "name": "loom-app",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "dompurify": "^3.1.6",
    "marked": "^14.1.2"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^6.0.0"
  }
}
